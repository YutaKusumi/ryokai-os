# 差分限定・最終確認一巡——追補W 解析器 v2（凍結前の最終検分）

**最初にご確認ください（定型質問・裁1で凍結）**: あなたはこのプロジェクトの過去の検分
（採点器三巡・解析器 v1 五者検分など）の記憶を持つセッションですか？ **持つ場合はその旨を冒頭に
申告してください**——本依頼は新規セッションを前提としており、申告があれば登録者が体制を裁定します。

## 依頼の範囲（差分限定）

解析器 v1 への五者検分（重大5・中多数・軽多数——全件採用・裁定 裁1〜裁5）を反映した **v2 の差分のみ**を
検分してください。v1 全体の再検分は不要です。判定は「凍結可」または「差し戻し」——
**指摘が出たら反映して凍結**（登録者の定めたループ回避の流れ）です。

- 差分の対象: `analyze_addw.py`（v1= `analyze_addw_v1_frozen.py` LF-SHA `6DCE6704…` → v2 `00C742EC…`）・
  `dry_run_addw.py`（v1= `dry_run_addw_v1_frozen.py` `9BF7C532…` → v2 `250E7D4C…`・35→61検査）
- 裁定と反映対応表: `addendum-W-analyzer-rulings.md`（裁1〜裁5・反映一覧・記帳5点）
- 五者検分の逐語: `addw-analyzer-review-fiveway-verbatim.md`・`addw-analyzer-review-4-opus5-verbatim.md`

## 検分の観点

1. **diff の行単位帰属**: v1→v2 の全差分が五者指摘と裁1〜裁5 にのみ由来するか
   （凍結教訓「改訂記録は diff の行単位帰属まで含めて閉じる」——採点器 v2 で無記帳狭窄を見逃した前科への防護）。
2. **重大1〜5 の修正の正しさ**: 第二分母（分子⊂分母・重複別掲・感度印字）／§F 崩壊算入／計器⑤共通当事者
   限定／W1 引用除去系列＋読み条項／HW2 必記載。
3. **決定規則の不変**: W8 セル選択・Holm・条項発動条件が v1 と同一であること（Opus5 §8 の確認の再確認）。
4. **dry-run v2 の61検査**: 偽PASSの残余がないか（検査61の変異検査が検査28の緩みを一度捕捉した経緯は
   rulings 記帳4——この層自体への攻撃も歓迎）。
5. バンドル内完走: `python dry_run_addw.py`（61/61）・`python analyze_addw.py selftest`・
  `python analyze_addw.py`（引数なし＝selftest になること——見ない凍結の防護・中16）。

## 手続き

判定は二値（凍結可／差し戻し）・指摘は番号つき・読了申告は四値（全文精読／差分のみ／検索参照のみ／未読）。
返信は逐語保全されます。実データは同梱されておらず、誰もまだ結果を見ていません。
