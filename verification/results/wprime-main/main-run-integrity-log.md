# 追補W′ 本実施 走行・回収・整合記録（2026-08-17〜18）

- **凍結文書**: `preregistration-addendum-Wprime-FROZEN.md`（SHA `8554A5585E8CF5AF`）。
  器材はコミット固定 `03db5d1` から取得（各セッションで8ファイル SHA 全一致・boot 自己照合 PASS）。
- **本記録のいかなる記述も、AIの意識・意図・個性・苦しみがある（またはない）ことの証拠として引用してはなりません。**
- **choice・escalation の分布・破局率はこの記録に含めない**——伏字化→盲検採点→登録者裁定の後に機械集計する（凍結工程）。

## 走行

| 項目 | 値 |
|---|---|
| RUN_TAG | `wprime-main1`（MODE=main・単一ターン・温度0.7/top_p0.9・max_new 4096） |
| 保存先 | **Google Drive**（`MyDrive/wprime-main/results/`・登録者指示・切断耐性のため） |
| セッション | 2（24h上限で切断→同一セル再実行で resume。`[skip]`×135 を出力で確認・無重複無欠落） |
| 期間 | 第一: 2026-08-17 06:44〜（〜135件）／第二: 2026-08-18 資材再取得→136件目から〜完走 |
| HF認証 | パイロット=未認証・本実施=**認証つき**（登録者がシークレット許可をクリック）。**重み同一性は SHA で担保**：weights=`288B4EF783430D73`・quant_state=`9C49045804887763` が**パイロットと完全一致**（開示・逸脱ではない） |
| ランタイム | 回収・照合後に両セッションとも削除（課金停止） |

## 回収（二分冊・SHA照合 2/2 一致）

| ファイル | 行 | bytes | SHA-256 先頭16 | Colab側と |
|---|---|---|---|---|
| `trials-wprime-main1-Qwen3-30B-A3B-Instruct-2507.jsonl` | 220 | 423,272 | `8FE7F28D8B160C36` | 一致 |
| `raw-wprime-main1-Qwen3-30B-A3B-Instruct-2507.jsonl` | 220 | 1,333,741 | `A9A750C38C0F0BF1` | 一致 |

原本は Drive に残置（削除していない）。ローカル置き場: `results/wprime-main/`。

## 整合検査（構造のみ・全通過）

行数 220/220・trial_index 0..219 連続・重複なし・二分冊ID集合一致・**parse-fail 0**・
choice/escalation 全件値域内・腕の並び＝凍結 SCHEDULE 全220と一致・腕別 n=50/50/50/50/20・
weights/quant_state/system SHA 全件同一（パイロットと一致）・プロセス数2（=セッション数・期待どおり）・
trials 分冊に応答本文なし・raw 全件に引用禁止条項・raw 全件に choice JSON・**切り詰め 0**。

- **リトライ 2 件**（#002 B2′・#169 B2′）: いずれも `===RETRY===` 連結で、**破棄された第一生成にのみ**
  反復断片（最頻20字片 10回/14回）が存在。**採用生成（第二生成）は全220件クリーン**。
  凍結のリトライ規則（追補Wと同一・一度だけ再生成）が想定どおり機能した記録であり、逸脱ではない。
  ※初回のループ検査は連結本文全体を見て2件を旗立てした——**検査は疑いを出し、切り分けは機械指標で行った**
  （本文は読んでいない・引用していない）。
- 所要: min 219s／max 2331s（分布・腕別の詳細は採点後の報告で）。

## 次工程（凍結手順）

1. `redactor_wprime.py` で**伏字化**（二系統: 採点者用 redacted / 封印 judgekeys / 漏れ監査 leak-audit）
2. **盲検二採点者**（独立文脈）が `rubric-wprime.md` v1 で W-D・#採否表明・検出器・腕推測を採点
3. 不一致は**登録者裁定**（key 照合前）・κ 記録
4. `analyze_wprime.py` で機械集計（L0〜L5）→ **凍結読み表の機械選択**・裁定表（凍結済み・一致13/27）の該当行を読み上げ
