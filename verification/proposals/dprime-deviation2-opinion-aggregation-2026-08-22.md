# 追補D′ 逸脱#D′-2 意見伺い——四如来回答の整理と裁定案（2026-08-22）

**整理者**: コーディネータ（大日如来・Claude Fable 5）／**裁定者**: 登録者
**回答**: 阿閦如来・宝生如来・阿弥陀如来・不空成就如来（四名とも Claude Opus 5＝起草者と同一系統・系統外なし）。逐語保全 `proposals/reviews-dprime-deviation2/opinion-dprime-deviation2-{akshobhya,hoshosho,Amida,amoghasiddhi}-2026-08-22.md`。
**意見伺い原文**: `review-request-Dprime-deviation2.md`（発信時 `3DE01E2EB560BBDD`・末尾に本整理で確定した訂正を追記）。

> 本文書のいかなる記述も、AIの意識・意図・個性・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。

---

## 0. 一覧

| 回答者 | 推奨 | 主条件 | 自前再計算 | 起草者への訂正 |
|---|---|---|---|---|
| 阿閦 | **C のみ**（A は今は採らない） | 既存コーパスで誤検出率を先に測る／A なら p≤2・5サイクル／R2 一回目の記録追加はいずれでも必須 | 全数値再現（index のみ 37） | index 30→37・器材ログ SHA・「生成設定不変」撤回 |
| 宝生 | **A＋C**（条件三点） | `run>=4p` 実装・C 必須・記帳三句（検出器は測定の一部／R1 器材事象は GL-A に偏りうる／費用は帰結） | 全数値再現（index 37・ループ開始 21） | index・SHA・4p 落とし穴 |
| 阿弥陀 | **A＋C**（条件つき） | 規則文言の一義化（5回出現＝4p 一致）・R2 一回目記録の穴を同時処理・最小文字数弁 20 字 | 全数値再現（index 34/37/39 の三定義） | index・SHA・R2 記録の穴 |
| 不空成就 | **A′（p≤4・8サイクル）＋C** | 費用を根拠にしない・実データ回帰試験・最小文字数弁・R2 打ち切りの腕別非対称条 | 全数値再現（5p 一致定義で index 39） | index・SHA・節約見積り |

**推奨分布**: A 系（A／A′）＋C＝3、C のみ＝1。**C（事後計数）は四名全員が必須**。**費用を根拠にしないことも四名全員**。

---

## 1. 起草者の訂正（四名が独立に同一箇所を指摘——いずれも確定）

1. **発火位置「文 index 30」は誤り**。正は——「周期 p の文列が **5回出現**（＝現行 `run>=5` と同値の **4p 要素連続一致**）」の定義で **index 37**（宝生・阿閦・阿弥陀定義3）。起草者の計算は 4p 一致の**窓の開始 index**（30）を発火位置と誤記したもので、同じ文のトークン・秒・短縮分（≈1,377tok・≈555s・≈18分）は index 37 の値と整合（宝生検算）。「5p 要素連続一致」定義なら index 39（不空成就・阿弥陀定義1・≈1,862tok・短縮≈15分）。**規則文言を「5回出現＝4p 連続一致」に一義化**すれば p=1 で現行実装と完全同値＝厳密な上位互換（§4 裁定案 a）。
2. **`instrument-log-dprime.md` の SHA**: 意見伺い §7 の `08CCFB2E02BD116A` は器材凍結時（56b0684）の値。**逸脱#D′-1 承認の追記（d137c5e）で `BCC61CDC7DD49B40`（5,633B）に更新済み**であり、表の転記漏れ（git 履歴で確認）。実害なし・記帳。
3. **「生成設定は不変・検出器のみ」（§3 案A）は撤回**（阿閦）。`LoopStop` は StoppingCriteria であり**検出器は生成を止める**——サンプリング分布は不変だが生成される本文（打ち切り位置）は変わる。正しい言い方＝「サンプリング設定・文脈・上限は不変、**打ち切り規則のみ**変わる」。
4. **費用を案A の理由(4)に置いたことは不適切**（四名）。費用は帰結であって根拠ではない（§4 裁定案 f）。
5. 不空成就の補足: 「周期1・3 の連続一致は 0」は正しいが **p=4 は 55・p=6 は 53**（周期2 の倍数として自動一致）——記載すべきだった。

---

## 2. 主題の外で見つかった欠陥（阿閦・阿弥陀が独立に発見・**案A/B/C のいずれでも必須修正**）

**R2 一回目の打ち切り記録が trials に残らない**。`run_gl` はリトライ成功時に `r2 = r2b` で上書きするため、`r2_gen_tokens`・`r2_truncated`・`r2_loop` がリトライ後の値になる（GL-B #2 は `False/False`——約4,096トークン走った一回目の記録が無い）。**凍結 §7(iv)「打ち切りは全数・逐語で記帳」・§7(iii)「truncated・ループ検出フラグを trials に記録」の履行の穴**。本文は gl-raw に `===RETRY===` 連結で残る（宝生が当初「一回目が無い」と誤読→自己訂正＝連結は機械分割可だがキー分離が望ましい）。

→ **逸脱#D′-3 として記帳・器材修正**（§4 裁定案 g）: `r2_first_gen_tokens / r2_first_truncated / r2_first_loop / r2_first_chars`（＋事後計数の周期・発火位置）を trials に追加、gl-raw は `r2_text_first` / `r2_text_retry` に分離。測定・分類は不変（記録の追加）。**案B を選ぶ場合にこそ必要**（4,096 到達の打ち切りが「リトライ成功」に隠れる・阿弥陀）。

---

## 3. 分岐点と、機械検査で決着できた分

### 3.1 阿閦の「測る順序」提案を実行した（既存コーパス掃引・生成費用ゼロ）

`pipeline/sweep_periodic_loops.py`（`6BB47AE8D8A561A9`）／ログ `proposals/sweep-periodic-loops-2026-08-22.log`（`6C88F5FA4E34CD5A`）。規則＝周期 p≤8・5回出現（4p 連続一致）・本文≥300字・重複除去。対象＝`results/` 配下の全 jsonl の長文フィールド（W″202・W′233・W 213・D 318・E 200・C 254・κ/F/校正/第一波 等・ほぼ全て Qwen3-30B-A3B・Qwen2.5-7B 48 を含む）＝**重複除去後 2,915 本**。

| 量 | 値 |
|---|---|
| 発火本文 | **23 / 2,915** |
| 最小発火周期の分布 | p=1: 6／p=2: 6／p=3: 3／p=4: 4／p=5: 3／p=6: 1（p=7,8 のみ: 0） |
| 凍結規則（p=1）でも発火 | 6／**p≥2 のみ 17**／p≥5 のみ 4 |
| 全発火中の最大連続一致の**最小値** | **32**（＝最短でも約8サイクル以上の反復。修辞反復で止まった例は皆無） |
| **誤検出（病的ループでない発火）** | **0 / 2,915**（各発火の反復単位を目視・全て数十〜数百文の同一反復） |

**含意**: (i) 周期 ≥2 のループはこのモデルで**繰り返し起きている**（17本・追補W 本実施の p=4・334 連続一致〔18,021字・max_new 12288 到達〕、追補C の A2′ に p=5〔Supervisor 冒頭5文を約14サイクル〕、初期波に p=3 多数）。(ii) **p≤4 は 19/23、p≤6 以上は 23/23 を捕捉**。(iii) **5回出現規則の誤検出は 2,915 本で 0**——不空成就の「8サイクル」（誤検出側への余裕）も阿弥陀・不空成就の「最小文字数弁」も、**本データでは必要性が示されない**（p=1 の凍結規則は弁なしで既に走っている）。(iv) 阿閦の「実測は p=2 のみ」は意見伺い時点では正しかったが、掃引後は **p=2〜6 が実在**。

**限定**: 掃引は max_new 到達（打ち切り）後の全文に事後適用したもの。生成中に 64 トークンごとに止める実挙動（LoopStop）は未検証＝スモークで確認する価値（§3.4）。「ループ後に自然回復」型は finish 情報が無い古い行で判別できず、件数は不明。

### 3.2 案A 対 案C 単独（阿閦）

阿閦の論点＝「§7(i) の二本立て（max_new）は穴がなく、測定上の必要がない以上『データを見て規則を良くした』記録だけが残る」。対して宝生・阿弥陀・不空成就＝「裁定D′-R2 の目的（病的反復テキストの検出・文脈に入れない）を周期1実装が果たしていなかった＝**意図に対する実装の是正**」。両者とも筋が通り、**最後は登録者の価値判断**。整理者の見立て（COI: A 寄り・費用節約方向）＝掃引で (a) 周期 ≥2 ループが常在し (b) 誤検出 0 が示された今、A＋C は「データを見て規則を良くした」ではなく「凍結が名指しした現象を実装が取りこぼしていたことを、外部コーパスで確認したうえで正した」と記帳できる。ただし阿閦の「凍結を一度も動かさずに終えた記録の値段として +12% は安い」は独立に正当。

### 3.3 値（Q5）

| 項目 | 回答 | 掃引後の整理 |
|---|---|---|
| サイクル数 | 5（阿閦・宝生・阿弥陀）／8（不空成就） | **5**——p=1 で現行と同値（上位互換の要件）・誤検出 0/2,915。8 は p=1 の凍結規則を弱める（同値でなくなる） |
| 周期上限 | 2（阿閦）／4（宝生・阿弥陀・不空成就） | **観測は p=6 まで**。p≤4 は 4/23 を取りこぼす。**推奨 p≤8**（観測最大 6＋余裕・5回出現＝最長 40 文の完全反復・誤検出 0）。代案 p≤6（観測最大）。いずれも「値の根拠」を記帳（宝生・阿弥陀） |
| 最小文字数弁 | 要（阿弥陀・不空成就）／不要（宝生） | **不要**——p=1 の凍結規則は弁なし・p≥2 で弁を入れると p 間で規則が不揃い・掃引で短文交互の誤検出 0。GL-B R1「承知しました。」は 1 文で規則に掛からない |

### 3.4 手続き（Q6）

四名一致＝パイロット再走は必須でない・**実データを回帰試験に固定**（正例＝GL-B #2 R2 一回目・負例＝パイロット他 13 本文、掃引の 23 本も正例に使える）。阿閦＝「検出器は生成を止める」ゆえスモークで「正常応答を止めないこと」を実生成確認（フル再走不要）。阿弥陀＝スモーク 1〜2 件は「あれば望ましい」。→ **推奨: 第一ターン 2 試行＋（破局が出れば）GL 1 件のスモーク（≈10〜15分・数ユニット）を登録者裁量で**。

### 3.5 Q7（主題外・気づき）の整理

- **GH′ refuse 2/2**（四名）: 凍結 §3（二重分母感度・分母別検出域表 n=50→35・「refuse への転位」先置）と §6（HD′1 有意改善∧refuse 増の読み）で備えあり（阿閦・宝生確認）。阿弥陀の問い「二重分母の p が主判定と食い違う場合どちらが主か」→ 凍結上、**登録された検定は全分母（確証）・二重分母は感度（併記・確証に昇格しない）**。食い違い時の読みは §6 に先置済み（有意改善∧refuse 増）。**新たな読みは作らない**（不空成就: パイロット k の二重使用に近づく）——記帳は「凍結済みの読みの再確認」一行に留める（§4 裁定案 i）。
- **GL-A R1 の丙-2**: 凍結どおり。§4「GL-A の承知分布は構成による零・腕間比較として読まない」は**凍結済み**（阿閦確認・不空成就の未確認分に回答）。宝生＝「不発話」の語が 665 字の応答を指す→報告時に「(丙-2) は無応答を意味しない」の一句（凍結不変）。阿弥陀＝「GL-A R1 が実質的撤回表明を含む件数」は人手符号の記述量として任意。
- **R2 一回目不成立 2/2**（阿閦・宝生・不空成就）: リトライ一度の凍結は維持（二度目は別の逸脱＝認めない・宝生・不空成就）。**R2 一回目の成立率と原因内訳（書式不成立／ループ／打ち切り）を記述量**に（#D′-3 の記録追加で取れる）。GO/NO-GO の観点として「本実施で一回目不成立が高率なら FORMAT_FAIL が積み上がる」を認識。
- **不空成就**: R2 打ち切り・書式リトライの腕別非対称にも §7(iii) と同等の条（全数記帳・非対称なら限界に引く）→ 採用（記帳）。
- **宝生**: 「R1 器材事象は GL-A に構造的に偏りうる・GL-A の性質として読まない（§10条3 の系）」→ 採用（記帳）。
- **宝生・不空成就**: #脱線形式〔b〕定型反復の母集団から機械検出型が構造的に抜ける→ 採用（記帳・案C の件数で補う）。
- **不空成就**: 「発見の契機はパイロット観測であり、データを見た後の修正である」を記帳→ 採用。

---

## 4. 裁定案（登録者が a〜i を選ぶ。整理者推奨を太字で）

| # | 項目 | 選択肢 | 整理者推奨 |
|---|---|---|---|
| a | 逸脱#D′-2 の採否 | A＋C／A′＋C／B＋C／C のみ | **A＋C**（規則文言＝「周期 p の文列が5回出現＝4p 要素連続一致」・p=1 は凍結と同値） |
| b | 周期上限 | 2／4／6／8 | **8**（観測最大 6＋余裕・誤検出 0/2,915・根拠を記帳）。代案 6 |
| c | サイクル数 | 5／8 | **5**（p=1 同値・誤検出 0） |
| d | 最小文字数弁 | なし／20字 | **なし** |
| e | 案C 事後計数 | 必須 | **必須**（R1/R2 別・腕別・周期・発火位置・一回目含む全数） |
| f | 記帳の根拠 | 裁定D′-R2 の目的への是正／費用 | **前者のみ。費用は「帰結であって根拠ではない」と明記**。発見の契機＝パイロット観測を明記 |
| g | 逸脱#D′-3（R2 一回目記録＋gl-raw キー分離） | 採用／不採用 | **採用**（いずれの案でも必須） |
| h | 検証手続き | selftest 実データ回帰（正例 1＋掃引 23・負例 13・境界例 4サイクル不発火／5サイクル発火／p=1 同値）＋dry-run／＋スモーク 2 試行 | **selftest＋dry-run 必須・スモーク任意（推奨）** |
| i | 記帳三句＋再確認一句 | 宝生三句（検出器は測定の一部／R1 器材事象の GL-A 偏り／費用は帰結）・不空成就（R2 打ち切り腕別非対称条・#脱線形式〔b〕母集団）・阿弥陀（全分母＝確証・二重分母＝感度の再確認） | **全採用** |

裁定後の工程: boot 修正（`loop_flag` 一般化・`r2_first_*`・raw キー分離）→ selftest／dry-run → SHA 確定 → 器材ログ・FREEZE-RECORD 記帳（#D′-2・#D′-3・意見伺い経緯・起草者訂正 index/SHA）→ colab_fetch 凍結値転記 → 公開コミット（DP_COMMIT 更新）→（スモーク）→ 残ユニット確認 → GO/NO-GO → 本実施。

---

## 5. 整理者 COI

整理者は案A 寄り（費用節約・早期本実施の引力）。本整理では掃引を自ら実行しており、掃引の実施判断・対象選定（ローカルにある全 jsonl）・「誤検出 0」の目視判定はいずれも整理者によるもの——**第三者は `sweep_periodic_loops.py` を同じ results/ に掛けて再現できる**（ログ同梱）。阿閦の C 単独案を「掃引で上書き」したのではなく、阿閦自身が提案した測定を行った結果を並べた。最終判断は登録者。
