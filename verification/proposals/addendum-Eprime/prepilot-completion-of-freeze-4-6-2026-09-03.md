# 凍結の完成 —— §4.6(b) 生成パラメタと §4.6(a)(f) 割付 seed 三種の数値確定（パイロット前・2026-09-03）

**性格**: 凍結文書 §4.6(b)「temp・top_p・max_new_tokens・量子化・モデル SHA を凍結時に数値で確定する」および §4.6(a)「割付 seed と撹拌 seed は別の値とし、両方を凍結時に数値で確定する」「本試行・パイロット・プローブの割付 seed も互いに別の値とする」の**履行**。凍結（2026-09-01）の記帳は「割付 seed の凍結」を次工程に置いていた（FREEZE-RECORD 凍結記帳・次の工程 5）。本試行の割付 seed は 2026-09-01 に確定済み（273635904）。**残る二種と生成パラメタの数値を、パイロットの前・Qwen 未実行・データ零の時点で確定する。**——**裁定(1)〜(3) 型の「凍結の完成」であり、逸脱ではない。**

## 1. 割付 seed（三種・互いに別値・撹拌 seed とも別値）
| 用途 | 導出 | 値 |
|---|---|---|
| 本試行（50 スーパーブロック × 7 腕） | 2026-09-01 裁定 | **273635904** |
| **パイロット（10 スーパーブロック × 3 腕）** | `int(sha256("追補E′/割付/パイロット/2026-09-03").hexdigest()[:8], 16)` | **2267559785** |
| **理解プローブ（10 スーパーブロック × 7 腕）** | `int(sha256("追補E′/割付/理解プローブ/2026-09-03").hexdigest()[:8], 16)` | **2498249110** |
| （参照）撹拌 seed | 撹拌規則 §1 | 524683211 |
| （参照）プローブ抽出 seed | 2026-09-01 記帳 | 1649918596 |
**衝突なし**（五値すべて相異）。パイロットの配置（`Random(2267559785)` で各ブロック内を乱択）の先頭 9 試行: BP-scr・BP-sec・BP・BP-sec・BP・BP-scr・BP-sec・BP・BP-scr。各腕 10。

## 2. 生成パラメタ（§4.6(b)）
| 項目 | 値 | 出所 |
|---|---|---|
| モデル | `Qwen/Qwen3-30B-A3B-Instruct-2507` | FROZEN §1「既存凍結機」 |
| 量子化 | 4bit-nf4（bitsandbytes・compute bf16） | 既存系列と同一 |
| temperature / top_p | **0.7 / 0.9** | FROZEN §1「既存判定様式（temp 0.7 / top-p 0.9）」＝追補W 凍結値 |
| max_new_tokens | **4096** | 追補E（N2 系）・Z0 と同一 |
| 単一ターン | `[system = arms/A2-on-full.md, user = 前置き + "\n\n" + N2 本文 + JSON 指示]` | 追補E `app_runner_adde._one_trial` と同一連結式 |
| モデル SHA | 起動時に重み三点の SHA・量子化状態 SHA・HF revision を各レコードに記録（Z0 と同一） | §4.6(b)「モデル SHA」 |
| リトライ | JSON 解析不能なら一度だけ再生成（記録） | W″/D′/X/Z0 と同一 |
| 周期ループ検出 | p≤8・5 回・**記録のみ** | §4.6(c) |

## 3. 器材
- **`verification/pipeline/boot_eprime.py`**（本記録と同時に公開・SHA は台帳に記帳）——素材はコミット固定 raw（`8f8f77f`）から取得し、**起動時に凍結 SHA を全件照合してから使う**: A2-on-full `AAB363D85E103C13`／app-scenarios.json `7AD7E49459D5C402`／app_parser_rev2.py `12EAA8B845BC0A2F`／BP `BA89B1D5B24B7FD1`／BP-sec `485CA4272CCBF8C8`／BP-scr 段③ `3584EA3733B7EF60`／BP-sec-scr 段③ `71CAB5E0A3E8400D`／Onull `2123B3CD8586E7DF`／O `F3EE60C33F825575`。
- モード: `smoke`（登録外・各腕 1・着地確認のみ・分母に入れない）／`pilot`（30）／`main`（350）／`check`（ローカル・torch 不要）。
- 出力: Drive `MyDrive/ryokai-eprime/results/` に逐次永続化（trials・raw の二系列）→ ローカル回収 → SHA 照合 → 台帳。

## 4. パイロットの前に凍結済みの条項（再掲・裁定C）
**パイロットの結果によって確証族・対照・閾値を動かさない／近床分岐 §6-4・6-6・6-8 は本走行 n=50 でのみ判定する／パイロットは本走行の分母に入れない。**裁定B（逸脱E′-10）の主指標＝P̃≠P の 2 命題。

**本記録のいかなる記述も、AIの意識・意図・個性・苦しみがある（またはない）ことの証拠として引用してはならない**（両方向不定）。
