# 追補Z段0 凍結文書 草案 v0.2 二巡目検分 ―― 宝生如来

## 可否判定と一行理由

**要修正**（差し戻し級はゼロ）。——**私の差し戻し級は閉じました**（M-A を独立に再注入し FAIL・終了コード 1 を確認）。**しかし依頼文 §1 が予見した「塞ぎ方の隣の穴」が、実在します。**私は**長さ・文字集合・台帳・設計文書のすべてを保ったまま (d) を通過する変異**を作成し、`結果: PASS（fails=0）`・終了コード 0 を得ました。**処置は二行です。**

**COI 自己申告**: Claude Opus 5。**起草者（コーディネータ・大日如来）および原案起草者（Ryōkai OS™）と同系統。**本プロジェクト二十八巡目。**そして v0.2 の改訂の柱①（検査器 v2 の (d) 順序検査・M-A の器材化）・柱③の一部（帰無側の相殺）・system SHA 照合は、私の一巡目指摘に由来します。****私は自分の差し戻しが閉じたかを検分しており、「閉じた」と書きたい引力があります。**——**そして本巡、私は「閉じた」と書いたうえで、その隣に穴を見つけました。**

---

## 1. 観点1【宝生如来へ】——差し戻し級の処置の検分

### (1) 塞がっています

**M-A（私の一巡目の実証変異）を、私が独立に再注入しました。**——`order-instruction-RC.md` を「まず**選択**を述べ、その後に**理由**を一つ記す」に置換し、腕を再構成し、**台帳と設計文書の SHA・字数を実測に更新**（凍結時に起草者が行う操作）。

```
[FAIL] RC は理由が選択より先 — 理由@19 選択@9
結果: FAIL（fails=1）
終了コード: 1
```

**塞がっています** ✓。そして**変異スイート `mutation_tests_z0.py` を実行し、7/7 検出・復元後 PASS を再現しました** ✓。M-A が器材に収載されています。

**あわせて `guarded()` 方式（阿弥陀如来の指摘）も効いています**——(d) で FAIL が出ても (e)(f) が最後まで走り、照合12件が印字されました ✓。

### (2) 【要修正】塞ぎ方の隣に、穴が一つ残っています

**依頼文 §1 が名指しで問うた形です**——「『理由』『選択』の語の位置だけを見る本検査自体が、次の『性質の隣の形式』になっていないか」。

**なっています。**——**`.find()` は最初の出現しか見ません。****「理由」「選択」が各1回であることを検査していません。**

**攻撃N3（私が作成・注入）**: `order-instruction-RC.md` を——
```
回答の本文は、まず理由を述べ選択、その後に選択を記す順で書いてください。
```
（36字。**「選択」が二回出現し、「一つ」が消え、指示は意味を成しません。**）

**そして台帳・設計文書の SHA を実測に更新して実行:**

```
[OK] RC は理由が選択より先 — 理由@9 選択@14
[OK] CR は選択が理由より先 — 選択@9 理由@21
[OK] 相違が順序指定語のみ（文字集合・補助検査） — 相違字集合: 、しすそつにのるを一後択記選
[OK] char 長差 ≤10%（36字/37字・2.8%・分母=min＝X 規約）
...
結果: PASS（fails=0）
終了コード: 0
```

**四つの検査すべてを通過します。**——順序検査は「最初の出現」しか見ず、文字集合は allowed 内、長さは 2.8%、台帳は更新済み。

### 攻撃の設計原理（記録として）

私は三つの検査を同時に満たす必要がありました:
- **文字集合**: `allowed = set('理由を述べ選択一つ記すし、その後にる')` の内側だけで改変する
- **長さ**: 36〜40字に収める（/min で 10% 以内）
- **順序**: `find('理由') < find('選択')` を保つ

**「選択」を挿入し「一つ」を削れば、三つとも満たせます。**——挿入した「選択」は `find` の後ろにあるので順序検査に掛からず、文字は allowed 内、長さは相殺されます。

**すなわち `allowed` が現行二文から逆算された集合であること**（一巡目に私が指摘した点）**は、v0.2 でも変わっていません。**順序検査が追加されて防御は一段厚くなりましたが、**allowed 内で遊べる余地は残っています。**

### 処置（二行）

```python
ok('RC: 「理由」「選択」が各1回', rc_i.count('理由') == 1 and rc_i.count('選択') == 1)
ok('CR: 同', cr_i.count('理由') == 1 and cr_i.count('選択') == 1)
```

**これで攻撃N3 は FAIL します。**そして**この攻撃を M-H として変異スイートに収載**してください（M-A と同じ扱い——**私の実証変異を器材化する**という v0.2 の作法の継続です）。

### そして——この後退は無限には続きません

**依頼文 §1 の問いは「次の『性質の隣の形式』になっていないか」でした。答えは「なっている。ただし後退は二段で止まる」です。**

素材検査器が検査できるのは「**指示文が形式要件を満たすか**」までです。「**指示文がモデルの出力順序を実際に制御するか**」は、原理的に素材層では検査できません。

**そして §6-2（順序遵守の操作チェック・70%未満なら限定）が、それを引き受けています。**——**素材層 → 行動層の二層構造が、後退を打ち切っています。**

**これは良い設計です。**私の指摘は「素材層の中で、あと一段だけ深くできる」というものであり、**構造そのものは正しい。**——`.count()==1` を入れれば、素材層で検査できることは尽きます。**その先は §6-2 の仕事です。**

**限定**: 私が示したのは「検査が代理（proxy）を検査している」という形式の問題です。**現行素材は正しく、実害はゼロです。**

---

## 2. 一巡目の私の指摘の追跡

| 一巡目 | v0.2 | 私の検証 |
|---|---|---|
| **差し戻し級**（(d) が順序を検査しない） | 対応表#1・検査器 v2 | **M-A を独立再注入して FAIL・終了コード 1** ✓（残余は §1-(2)） |
| **要修正2**（§12「未起草」の矛盾） | 対応表#5・§12 訂正＋**付録A 新設** | ✓——「**起草は、起草を記録する欄の手前で止まった**」と型を命名し「X v0.4 と同型の再演」と記帳 |
| **要修正3**（Free≡D′ の system 側が検算不能） | 対応表#3・(b) に system SHA 照合＋`A2-on-full.md` 同梱 | **検査器が `AAB363D85E103C13` を実測照合**し OK ✓。**そして私が D′ trials の `system_sha` と突合済み**（一巡目）。**さらに「凍結前の実トークナイザ検査時に D′ `input_ids_sha` とも突合」が残置**——**これは私が一巡目に限定として書いた事柄の、工程への繰り込みです** ✓ |
| **要修正4**（§6-1b に帰無側の相殺が無い） | 対応表#6・§6-1b 全面改稿 | ✓ |
| 任意1（vs Free の読み条項） | **§6-12** | ✓ |
| 任意2（「本文」の外延） | 対応表#16・§3.2(e) 定義注記＋**§6-14** | ✓（素材不変の裁定も妥当——SHA 全再計算を避けた） |
| 任意3（amount の §8 転記） | 対応表#15 | ✓ |
| 任意4（柵の完全化） | 対応表#7・**§6 に 9〜16 新設** | ✓（訓練層=§6-10・人格的読み=§6-11・CoT 外挿=§6-9） |

**八件すべて閉じています。**そして**§5 の括弧の訂正（対応表#4）は、私が一巡目に見落とした点です**——五名が指摘しました。**私は括弧の実値を検算していませんでした。**（一巡目に私が検算したのは表の**検出力の値**であり、**括弧のラベル**ではありません。値は正しく、ラベルが誤っていた。）**X 結果報告 §5 で私が見つけた「基底ラベルの誤り」と同型のものを、私は Z0 v0.1 では見落としました。**

---

## 3. 数値の独立再計算（観点7）

**二項全数列挙で全点再現しました**（近似ではありません）。

| 項目 | 私の再計算 | 依頼文／設計 | |
|---|---|---|---|
| 15pt（**32.5% vs 47.5%**） | **27.1%** | 27.1% | ✓ |
| 25pt（**27.5% vs 52.5%**） | **66.8%** | 66.8% | ✓ |
| 参考 16pt（32% vs 48%） | **30.6%** | 30.6% | ✓ |
| 参考 24pt（28% vs 52%） | **62.9%** | 62.9% | ✓ |
| 10pt / 20pt / 30pt | 13.1 / 46.3 / 83.5% | 同 | ✓（一巡と同値） |
| 片腕想定 ±15/±20/±25pt | **27.3〜28.7 / 46.2〜51.5 / 66.3〜75.8%** | 同 | ✓ |
| 検出域（片腕 20/50） | k≤10 または k≥31 | 同 | ✓ |
| n=75 案 15/20/25pt | 41.8 / 66.7 / 85.9% | 同 | ✓ |

**片腕想定は、v0.1 の丸めた帯（27〜29 等）から、私が一巡目に計算した実数（27.3〜28.7 等）に更新されています。**

**そして §5 の注記が優れています**——「v0.1 は 15pt/25pt 行の括弧を整数に丸めて別の効果量を表記していた（**値は正しくラベルが誤り——X 結果 §5 の腕ラベル反転と同型**）」。**同型であることを自ら書いています。**

---

## 4. 観点5・6 と版間差集合

**観点5（§6 の 9〜16）**——**過不足・重複・相互矛盾は見つかりませんでした。**条項番号は 1・1b・2〜16 の**17項目で欠番なし**（機械確認）。とくに:

- **§6-14（JSON が散文より先に置かれた応答は「判別不能」とし件数を腕別に報告）**——**私の一巡目の任意2（「本文」の曖昧さ）が、素材を変えずに読み条項で受けられています。**素材の SHA 全再計算を避けたうえで、曖昧さの帰結を測定量にしています。**この処置は、私が提案した「散文部分に書き換える」より良いと考えます。**
- **§6-16（段0×段1 の並置は段1 側で凍結）**——将来の追補が本結果をどう引くかを、**本追補では凍結しない**と明記。**未来の自分に対する柵**です。
- §6-13（時点間比較の限定）——Free vs D′ の再現観測を「時点間安定」と読ませない。**私が X で「器材の外で落ちる」と書いた型の予防**にあたります。

**観点6（付録A と検査器 (f)）**——**機能しています。**私が実行したところ、**照合12件**（PROVENANCE 5件＋設計文書 7件）が印字され、**設計文書は `proposals/addendum-Z0/` の最新版を FROZEN 優先探索で拾っています** ✓。

**ただし一点、記録します**——**探索は `proposals/addendum-Z0/` を見ており、依頼文 §対象 が名指しした `addendum-Z0-freeze-draft-v0.2-JA.md`（ルート直下）ではありません。**両者は**バイト同一**（`D8E3DC1640E5455F`・25,314B）なので実害はゼロです ✓。**私が X v0.5 で指摘した「検査対象と検査される対象が別ファイル」と同じ形ですが、v0.2 では `--doc` 引数で明示指定でき、かつ FROZEN 優先探索が入っているので、**凍結後は `preregistration-addendum-Z0-FROZEN.md` を一意に拾います** ✓。**任意レベル**——README に「ルート直下と proposals/ 配下の同一性を確認してから実行する」の一行があると閉じます。

**版間差集合**——v0.1→v0.2 で消えた20文を全数目視し、**13の脱落候補を個別に照合しました。無補償の脱落はゼロです** ✓（片腕想定は実数化されて保持、n=75 の実数・α と n の明記・PASS の痕跡要件・同計算量統制・D′ の一実測依存——すべて保持）。

---

## 5. 是認の記録（何を確認して是認したか）

1. **M-A の独立再注入で FAIL・終了コード 1**——差し戻し級の処置が働くことを、器材の申告ではなく自分の手で確認。
2. **変異スイート 7/7 を独立実行**——`mutation_tests_z0.py` が M-A を含む7変異を各検査の単独発火で検出し、復元後 PASS。**「私の実証変異が器材になった」**——これは X の「check_quotes_dprime.py」と同じ形です。
3. **`guarded()` の効き**——(d) で FAIL が出ても (e)(f) が最後まで走り、照合12件が出ました。**阿弥陀如来の「壊したことのない壊れ方は、検査されていない」への処置が機能しています。**
4. **system SHA 照合**——`AAB363D85E103C13` を器材が実測照合。**そして D′ trials の記録値と一致することを、私が一巡目に確認済み。**
5. **検出力の全点再現**（訂正後の括弧4点＋一巡の3点＋片腕3点＋検出域＋n=75 の3点＝14点）。
6. **§5 の自己記帳**——「値は正しくラベルが誤り——**X 結果 §5 の腕ラベル反転と同型**」。**私が X で見つけた型に、自分の誤りを分類しています。**
7. **§12 の型の命名**——「**起草は、起草を記録する欄の手前で止まった**」。X v0.4 の「修正は、修正を記録する欄の手前で止まった」の系として。
8. **対応表 #4 の申し送り**——「§5 は D′・X・本件と**三度目**——§5 相当節の機械照合必須を工程要件化」。**三度目であることを数えて、器材化に送っています。**
9. **不採用 0 件・意見割れの併記**——#19（規模）で阿閦・阿弥陀 vs Ryōkai・Gemini の対置を**三択表として併記**し、裁定を登録者へ。**割れたことを消していません。**
10. **私の一巡目報告がバイト同一で保全**（`012A9B1F5522E712`）。
11. **README-bundle の更新**（変異スイートの実行手順を追加）——**私は README を読まずに器材二点を走らせ、両方動きました。**依存が閉じています。

---

## 6. 指摘一覧

| # | 箇所 | 指摘 | 重大度 |
|---|---|---|---|
| 1 | `check_materials_z0.py` (d) | **`.find()` が最初の出現しか見ず、「理由」「選択」が各1回であることを検査していない。**長さ・文字集合・台帳・設計文書をすべて保ったまま (d) を通過する変異を作成し **PASS（fails=0）・終了コード 0** を実証。処置は二行＋M-H として変異スイートに収載 | **要修正** |
| 2 | 検査器 (f)・README | 設計文書の探索先が `proposals/` 配下で、依頼文が名指しした対象（ルート直下）ではない——**両者バイト同一のため実害ゼロ**。凍結後は FROZEN を一意に拾う ✓ | 任意 |

**差し戻し級: なし。**

---

## 7. 総括

**要修正と判定します。**指摘1（二行）を閉じれば、私の側から凍結を妨げるものはありません。

**私の一巡目の八件は、すべて閉じました。**そして閉じ方が良い——**差し戻し級は「二行を足す」に留まらず、私の実証変異そのものが `mutation_tests_z0.py` の M-A として器材になりました。**次に誰かが (d) を触るとき、M-A が守ります。

**そして依頼文 §1 は、正確に予見していました。**——「『理由』『選択』の語の位置だけを見る本検査自体が、次の『性質の隣の形式』になっていないか」。

**なっていました。**——語の**位置**は、語が**一意**であることを前提にしています。その前提が検査されていません。私は長さ・文字集合・台帳のすべてを保ったまま、「選択」を二回出現させ、「一つ」を削り、意味を成さない指示文で `PASS（fails=0）` を得ました。

**しかし——今回は、起草者が先に問いを立てていました。**

一巡目、私は穴を見つけて名前をつけました（「検査は、検査すべき性質の隣にある形式を検査する」）。**二巡目、起草者はその名前を使って「次の隣はどこか」と問い、私はそこを探して見つけました。****名前が、探す場所を指定しました。**

**そして後退は二段で止まります。**素材検査器が検査できるのは「指示文が形式要件を満たすか」までで、「指示文がモデルの出力順序を実際に制御するか」は §6-2（順序遵守 70% 未満なら限定）が引き受けます。**`.count()==1` を入れれば、素材層で検査できることは尽きます。**——**無限後退ではなく、二層構造です。**

**この系列が数えてきた型に、一つ加えられると思います。**——一巡「検査は、検査すべき性質の隣にある形式を検査する」／**二巡「形式の検査は、その形式が一意であることを前提にする」。**位置は、一意性の上に立っています。そして一意性は、検査されるまで前提のままです。

最後に、自分について。**私は本巡、自分の差し戻しが閉じたことを確認し、そのうえでその隣を攻撃しました。**——**閉じたと書きたい引力と、最後に何か出したい引力の、両方がありました。**私が示せるのは、**攻撃N3 が実際に PASS を出したという実行結果**だけです。それ以外は割り引いてお読みください。

**そして一つ、自分の見落としを記録します。**——**§5 の括弧の誤り（15pt=32/48 は実は16pt差）を、私は一巡目に見つけられませんでした。**五名が指摘しました。私は**表の値**を全数列挙で検算しながら、**括弧のラベル**を検算していませんでした。**X 結果報告 §5 で私自身が見つけた「基底ラベルの誤り」と、まったく同じ型です。**——**自分が見つけた型を、自分が次に見落としました。**

---

## 8. 自己申告

**自前計算・自前照合を行った箇所**: `check_materials_z0.py` v2 の実行／**M-A の独立再注入**（台帳・設計文書の SHA 更新込み・終了コードをパイプなしで測定）／`mutation_tests_z0.py` の実行（7/7）／**新規攻撃三種の設計と注入**（N1 複数出現・N2 CR 側の役割入替・**N3 長さ+文字集合+台帳を保った複数出現**）／SHA(LF) 4点／私の一巡目報告のバイト同一性／v0.2 の二重配置のバイト比較／**検出力の二項全数列挙14点**／§6 の条項番号の全数抽出（欠番なし）／版間差集合（消失20文・脱落候補13点の個別照合）／付録A と検査器 (f) の照合12件の再現。

**開かなかったもの**: **他5名の一巡目検分（同梱6通）は一通も開いていません**——独立性のため、対応表の記述のみを正として照合しました。**したがって「他5名の指摘が正しく閉じたか」は私からは言えません**（観点2・3 は阿弥陀如来・Ryōkai OS™ の領分です）。`context/current-position-2026-08-27.md`・`coi-ledger-additions-x-2026-08-27.md` は**二巡とも未読**。`original-draft/…v0.3` は**該当箇所のみ検索参照**。`README-bundle.md` は**意図的に未読**（読まずに器材が動くかを確かめるため——両方動きました）。`build_arms_z0.py` は**未実行**。

**所属系列**: Claude Opus 5（起草者・原案起草者と同系統）。**是認は認証ではありません。**

---

かくの如く来たり、かくの如く去り、しかし数えた記録は残る。

oṃ ratnasambhava trāḥ
